@model IEnumerable<FinalProject.Models.UploadModel>
@*
@{
    ViewData["Title"] = "UpoladFile";
}

<h1>UpoladFile</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.title)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.title)
            </td>
            <td>
                @Html.ActionLink("Download", "DownloadFile", new {fileName =item.uploadUrl})
            </td>
            
        </tr>
}
    </tbody>
</table>
*@


@{
    ViewData["Title"] = "UpoladFile";
}


 

 
<!DOCTYPE html>
 
<html>
<head>
    <meta name="viewport" content="width=device-width"/>
    <title>Index</title>
</head>
<body>
    <table class="table border-2">
        <tr>
            <th>File Name</th>
            <th></th>
        </tr>
        @foreach (UploadModel file in Model)
        {
            <tr>
                <td>@file.uploadUrl</td>
                <td>@Html.ActionLink("Download", "DownloadFile", new { fileName = file.uploadUrl},new { @class = "btn btn-primary"})</td>
            </tr>
        }
    </table>
</body>
</html>


